<% if !user_signed_in? || current_page?(root_path)%>
<nav class="navbar is-fixed-top is-spaced has-shadow is-hidden-desktop" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <%if !user_signed_in? || current_page?(root_path)%>
      <a href="/" class="navbar-item th-700 f-25 has-text-primary">Persuasion Paper</a>
    <%end%>
  </div>

      <div class="navbar-item">
<% if !user_signed_in? %>
<div class="buttons">
  <%= link_to "Sign up", new_user_registration_path, :class => 'button is-primary navbar-item th-700'  %>
  <%= link_to "Login", new_user_session_path, :class => 'button is-light navbar-item'  %>
</div>
<% end %>
        </div>
  </div>
</nav>
<br class="is-hidden-desktop">
<br class="is-hidden-desktop">
  
<% end %>
<% if current_page?(posts_path) %>
  <% if !user_signed_in? %>
<nav class="navbar is-fixed-top is-spaced is-hidden-desktop" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
<% if !user_signed_in? %>
<a href="/" class="navbar-item"> <i class="material-icons f-20">chevron_left</i> Back Home</a>
<% end %>

  </div>
<div class="navbar-end">
<div class="navbar-item">
<div class="buttons">
  <%= link_to "Sign up", new_user_registration_path, :class => 'button is-primary navbar-item th-700'  %>
  <%= link_to "Login", new_user_session_path, :class => 'button is-light navbar-item'  %>
</div>
</div>
</div>
</nav>
<br class="is-hidden-desktop">
<br class="is-hidden-desktop">
  
<% end %>
<% end %>


<nav class="navbar is-fixed-top has-shadow is-spaced is-hidden-touch" role="navigation" aria-label="main navigation">
  <div class="navbar-brand">
    <%if !user_signed_in? || current_page?(root_path)%>
      <a href="/" class="navbar-item th-700 f-25 has-text-primary">Persuasion Paper</a>
    <%end%>
    <a role="button" class="navbar-burger burger" aria-label="menu" aria-expanded="false" data-target="navbarBasicExample">
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
      <span aria-hidden="true"></span>
    </a>
  </div>

  <div id="navbarBasicExample" class="navbar-menu">
    <div class="navbar-start">
    <a href="/posts" class="navbar-item <%if current_page?(posts_path)%> th-700 has-text-primary<%end%>">Question Feed</a>
    <a href="/posts/new" class="navbar-item <%if current_page?(new_post_path)%> th-700 has-text-primary<%end%>">New Question</a>
    </div>

    <div class="navbar-end">
    <% if user_signed_in? %>
      <%= link_to read_notification_path, method: :post, class: "navbar-item" do%>
        <i class="material-icons has-text-primary">notifications</i>
          <% if user_signed_in? && !Notification.where(user_id: current_user.id).blank? && current_user.read_notification < Notification.where(user_id: current_user.id).order(created_at: :DESC).first.created_at %>
            <i class="material-icons has-text-danger f-15">fiber_manual_record</i>
          <% end %>
      <% end %>
    <% end %>
      <div class="navbar-item">
<% if user_signed_in? %>
<div class="navbar-item has-dropdown is-hoverable is-left">
    <a class="navbar-link is-rounded">
        <strong>Profile</strong>
    </a>
    <div class="navbar-dropdown is-right">
      <%= link_to 'My Posts', my_posts_path, :class => 'navbar-item' %>
      <%= link_to 'Favorites', my_favorites_path, :class => 'navbar-item' %>
      <hr class="navbar-divider">
      <%= link_to 'Edit profile', edit_user_registration_path, :class => 'navbar-item' %>
      <%= link_to 'Admin dashboard', admin_root_path, :class => 'navbar-item' unless !current_user.admin? %>
      <%= link_to "Logout", destroy_user_session_path, method: :delete, :class => 'navbar-item'  %>
    </div>
</div>
<% else %>
<div class="buttons">
  <%= link_to "Sign up", new_user_registration_path, :class => 'button is-primary navbar-item th-700'  %>
  <%= link_to "Login", new_user_session_path, :class => 'button is-light navbar-item'  %>
</div>
<% end %>
        </div>
      </div>
    </div>
  </div>
</nav>
<br class="is-hidden-touch">
<br class="is-hidden-touch">

