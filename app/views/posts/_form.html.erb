<%= form_with(model: post, local: true) do |form| %>
  <% if post.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <label for="">Title for this post (100 characters)</label>
    <%= form.text_area :title, class: "textarea is-light", rows: "1", maxlength: 100, required: true %>
  </div>

  <div class="field">
    <label for="">The content for your thought or belief (600 characters)</label>
    <%= form.text_area :body, class: "textarea is-light", rows: "3", maxlength: 600, required: true %>
  </div>

  <div class="field">
    <label for="">Tags to help people find this post (100 characters)</label>
    <%= form.text_field :tags, class: "input is-light", maxlength: 100, required: true %>
  </div>

  <div class="actions">
    <%= form.submit "submit", class: "button is-primary" %>
  </div>
<% end %>